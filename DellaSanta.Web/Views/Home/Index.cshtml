@model IEnumerable<DellaSanta.Core.UploadedeFiles>

@{
    ViewBag.Title = "Home Page";
}

<br /><br />

<div class="panel panel-default">
    <div class="panel-heading">
        <strong>Upload your .docs file</strong>
    </div>

    <table class="table table-hover table-striped table-condensed small">
        <tr>

            <th class="col-md-1">
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th class="col-md-4">
                @Html.DisplayNameFor(model => model.NameOnDisk)
            </th>

    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameOnDisk)
            </td>


          
        </tr>
    }

    </table>
    
   
    @using (Html.BeginForm("Upload", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
    {
        <table>
            <tr>
                <td>File:</td>
                <td>
                    <input type="file" name="UploadedFile"  />
                </td>
            </tr>
            <tr>
                <td colspan="2">
                    <input type="submit" name="Submit" value="Submit" />
                </td>
            </tr>
        </table>
    }
    
 
</div>


